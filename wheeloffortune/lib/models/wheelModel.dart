import 'dart:ui';

//Make a class model for wheel and that will take two properties "Prize and Color"

class WheelModel {
  final String prize;
  final Color color;
  WheelModel(this.prize, this.color);
}
